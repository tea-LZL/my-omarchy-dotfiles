[ids]

*

[main]

# 1. Physical Caps Lock becomes Control (hold) and Escape (tap)
capslock = overload(control, esc)

# 2. Physical Escape becomes Escape (to keep it functional)
esc = esc

# 3. Define the magic combo: 
# While holding the 'control' layer (Caps Lock), pressing 'esc' toggles Caps Lock
[control]
esc = capslock
